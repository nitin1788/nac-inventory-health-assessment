File: `nac-pharmacy-inventory-consulting.webp` (converted from the delivered
`nac-pharmacy-inventory-consulting.webp.png`, which was actual PNG data). Genuine WebP now, plus
an `-800w.webp` mobile variant.

**2026-08-21: unwired, not deleted.** The on-screen laptop in this photo shows a fabricated
"INVENTORY HEALTH REPORT" with invented figures (Total SKUs 1,246, In Stock 992, Low Stock 162,
Out of Stock 92, plus a "Top Slow Moving Items" list with fake product names/counts) —
presented as if it were a real client deliverable, which conflicts with the site's
no-fake-statistics rule. A CSS crop can't fix this: the raw file is still directly and publicly
servable with the numbers fully legible. No longer referenced by `TwoVerticalsSection.tsx` (falls
back to an icon-only card header) — see `NAC_SEO_READINESS_AUDIT.md`.

Used by: `frontend/src/features/landing/components/TwoVerticalsSection.tsx` (Inventory & Operations Consulting card) — previously.
