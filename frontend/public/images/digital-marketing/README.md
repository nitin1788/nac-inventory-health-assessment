File: `nac-healthcare-digital-marketing.webp` (converted from the delivered
`nac-healthcare-digital-marketing.webp.png`, which was actual PNG data). Genuine WebP now, plus an
`-800w.webp` mobile variant.

**2026-08-21: unwired, not deleted.** The on-screen laptop in this photo shows a fabricated
"DIGITAL MARKETING PERFORMANCE" dashboard with invented numbers ("23,985" website visitors,
"1,257" leads generated, "₹42.5" cost per lead, a fake campaign-performance bar chart) —
presented as if it were a real result, which conflicts with the site's no-fake-statistics rule.
A CSS crop can't fix this: the raw file at this path is still directly and publicly servable with
the numbers fully legible. No longer referenced by `TwoVerticalsSection.tsx` or
`DigitalMarketingSection.tsx` (both fall back to text-only layouts) — see
`NAC_SEO_READINESS_AUDIT.md`. A real, clean digital-marketing photo (no on-screen fabricated
numbers) would restore the original two-column layout.
